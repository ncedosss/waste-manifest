{
  "name": "waste-manifest-app",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "cd waste-manifest-app && npm install && npm run build",
	"build": "react-scripts build"
  },
  "dependencies": {
    "express": "^4.18.2",
	"build": "react-scripts build"
  },
  "engines": {
    "node": ">=18"
  }
}
